// Archivo que define la estructura que tiene un arrendador (simplificado)

export interface Arrendador {
  id:number;
  nombre:string;
  usuario:string;
  contrasena:string;
  numCanchasRegistradas:number;
}
